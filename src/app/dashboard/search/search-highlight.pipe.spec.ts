import {SearchTermHighlightPipe} from './search-highlight.pipe'

describe('SearchHighlightPipe', () => {
    it('create an instance', () => {
        const pipe = new SearchTermHighlightPipe()
        expect(pipe).toBeTruthy()
    })
})
